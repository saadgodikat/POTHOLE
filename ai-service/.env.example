# ── Model ──────────────────────────────────────────────────────────────────────
# After running train.py, the fine-tuned model is auto-detected from models/pothole_finetuned.pt
# To use a different HuggingFace model as the base (before fine-tuning):
HF_REPO=Harisanth/Pothole-Finetuned-YOLOv8
HF_FILENAME=best.pt

# Explicit model override (optional — normally leave blank):
# MODEL_PATH=./models/pothole_finetuned.pt

# ── Detection ─────────────────────────────────────────────────────────────────
CONFIDENCE_THRESHOLD=0.30

# ── Training (for train.py) ───────────────────────────────────────────────────
# Get your Kaggle API key from: https://www.kaggle.com/settings → API → Create New Token
# Then place kaggle.json in C:\Users\YourName\.kaggle\kaggle.json
# OR set these env vars:
# KAGGLE_USERNAME=your_username
# KAGGLE_KEY=your_api_key

# Training hyperparameters (optional overrides):
# TRAIN_EPOCHS=50
# TRAIN_BATCH=8        (increase to 16 if you have a GPU)
# TRAIN_IMG_SIZE=640
